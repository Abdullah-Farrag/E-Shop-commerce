export interface Product {
    id: number;
    name: string;
    shortDescription: string;
    fullDescription: string;
    price: number;
    currency: string;
    image: string;
    inStock: boolean;
}